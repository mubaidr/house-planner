'use client';

export default function DoorAnimationControls() {
  // This component is now replaced by DoorFloatingControls
  // Keeping it for backward compatibility but returning null
  return null;
}